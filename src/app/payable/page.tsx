import { PayablePageClient } from '@/components/payable/payable-page-client';

export const dynamic = 'force-dynamic';

export default function PayablePage() {
  return (
    <PayablePageClient />
  );
}
